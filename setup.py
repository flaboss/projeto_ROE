from setuptools import find_packages, setup

setup(
    name='modules',
    packages=find_packages(),
    version='0.1.0',
    description='Project to compute stock options strategies',
    author='Flavio Bossolan'
)